<% content_for :title, "Navigations" %>

<%= link_to "New", %i[new menu], class: "button button--primary" %>

<%= table_with(collection:) do |row, menu| %>
  <%= row.text :title do |cell| %>
    <%= link_to cell, menu %>
  <% end %>
  <%= row.text :slug %>
<% end %>
